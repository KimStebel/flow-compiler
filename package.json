{
  "name": "flow-idris",
  "version": "0.0.1",
  "description": "flow integration for idris",
  "main": "dist/main.js",
  "scripts": {
    "flow": "flow",
    "build": "babel src/ -d dist/",
    "prepublish": "npm run build",
    "start": "npm run build; node dist/main.js",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "flow",
    "types",
    "idris"
  ],
  "author": "Kim Stebel",
  "license": "BSD-2-Clause",
  "dependencies": {
    "babylon": "^6.18.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-flow": "^6.23.0",
    "babel-preset-stage-2": "^6.24.1",
    "flow-bin": "^0.54.0"
  }
}
